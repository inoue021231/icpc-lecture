#include<bits/stdc++.h>
using namespace std;

int main() {
  while(true) {
    int N;
    cin >> N;
    if(N == 0) break;
    vector<string> s(N);

    for(int i = 0; i < s.size(); i++) {
      cin >> s[i];
    }
  }
  return 0;
}